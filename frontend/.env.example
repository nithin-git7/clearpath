# Optional external API origin. Leave blank to use the ClearPath service bundled with the site.
NEXT_PUBLIC_API_BASE_URL=

# Public canonical origin used by sitemap generation. No trailing slash.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
